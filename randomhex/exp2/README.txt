The results.txt file contains the following info on each line:

n & k & mean(n) & var(n) & P(n)

where n is the boardsize, k is the numbers of games analyzed, mean(n) is the 
average game length, var(n) is the variance of the gamelength, P(n) is the probability
that BLACK wins if WHITE played the first move.
