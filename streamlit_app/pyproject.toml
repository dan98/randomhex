[project]
name = "randomhex-app"
version = "0.1.0"
description = "Random-Turn Hex — Monte Carlo strategy visualisation"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.30.0",
]

[tool.uv]
package = false
